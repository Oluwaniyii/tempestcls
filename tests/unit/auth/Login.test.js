"use strict";

const mocha = require("mocha");
const { expect, assert } = require("chai");
const sinon = require("sinon");

describe.skip("Login", function() {
  describe("verifyPassword", function() {
    it("should return true when provided password matches user password", function() {});

    it("should return false when provided password does not match user password", function() {});
  });
});
